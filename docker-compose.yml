version: '3.10'

services:
 web:
  build:
   context: .
  ports:
  - 5000:5000
  volumes:
  - ./:/app

 db:
     image: "mcr.microsoft.com/mssql/server"
     ports:
      - 1433:1433
     environment:
         SA_PASSWORD: "P@ssw0rd!"
         ACCEPT_EULA: "Y"